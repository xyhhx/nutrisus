image: alpine/edge
packages:
  - docker
  - hut
secrets:
  - acff583f-01c1-4b56-9094-092bb140e6bd
tasks:
  - docker_login: |
      docker login -u xyhhx -p $(cat ~/.docker_hub_token)
  - build: |
      cd noodleapp
      docker build . -t noodleapp
  - publish: |
      docker login
